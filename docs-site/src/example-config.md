# Example Configuration

Copy this to `~/.config/navra/config.toml` and edit to suit your
setup. See [Configuration Reference](./configuration.md) for the
full option list.

```toml
{{#include ../../examples/config.toml}}
```
