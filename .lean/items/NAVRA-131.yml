id: NAVRA-131
title: Fix clippy warnings across workspace
status: done
priority: medium
effort: S
depends_on: []
feeds: []
description: |
  Fix the ~8 real clippy warnings (excluding 37 intentional kani cfg
  warnings):

  1. Complex types in navra-safety/src/ner.rs (lines 158, 638) and
     privacy_filter.rs (lines 48, 342) — extract type aliases
  2. Redundant closures in navra-server (~5 instances)
  3. &mut Vec → &mut [_] in navra-safety/src/lib.rs:583
  4. Loop variable indexing in navra-model/src/onnx.rs (237, 240)
     and navra-safety/src/projection.rs:107
  5. Prefix stripping in navra-safety/src/ner.rs:874
  6. Collapsed if/else in navra-safety/src/privacy_filter.rs:148
  7. Derivable impls (~4 instances)
  8. from_str method rename in navra-model/src/chat.rs:141
  9. Empty lines after doc comment in navra-protocol/src/permissions.rs:9
  10. contains() vs iter().any() in navra-safety/src/ner.rs:772

  Do NOT suppress kani cfg warnings — those are intentional for
  formal verification harnesses.
acceptance:
  - "cargo clippy --workspace produces only kani cfg warnings (no other warnings)"
  - "All tests pass after fixes"
scope:
  - navra-safety/src/ner.rs
  - navra-safety/src/privacy_filter.rs
  - navra-safety/src/lib.rs
  - navra-safety/src/projection.rs
  - navra-model/src/onnx.rs
  - navra-model/src/chat.rs
  - navra-protocol/src/permissions.rs
  - navra-server/src/
verification:
  quick: "ORT_LIB_PATH=/usr/lib64 ORT_PREFER_DYNAMIC_LINK=1 cargo clippy --workspace 2>&1 | grep -v kani | grep 'warning:' | wc -l"
progress: 100
