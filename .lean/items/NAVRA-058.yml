id: NAVRA-058
title: Negative constraints in persona schema
status: done
priority: low
effort: S
depends_on: []
feeds: []
description: |
  Add optional constraints field (Vec<String>) to Persona struct for negative instructions. Weaver renders as "## Constraints" section after mandate, before heuristics. Forge validates no empty strings. Backward compatible via serde(default).
acceptance:
  - "Persona struct accepts optional constraints: Vec<String> with serde(default)"
  - "Weaver assembles constraints as '## Constraints' section after mandate"
  - "Persona YAML without constraints key deserializes successfully"
  - "ForgeService warns on empty-string constraint entries"
  - "register_upstream_persona initializes constraints to Vec::new()"
  - "Round-trip deserialization and weaver assembly tests pass"
scope:
  - navra-cognitive/src/types.rs
  - navra-cognitive/src/weaver.rs
  - navra-cognitive/src/forge.rs
verification:
  quick: "cargo test -p navra-cognitive"
